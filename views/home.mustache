<!DOCTYPE html>
<html>
  {{>_head}}
  <body>
    <div class="container">
      {{#users}}
        {{>_user}}
      {{/users}}
      {{^users}}
        <p>Nothing to see here!</p>
      {{/users}}
    </div>  
  </body>
</html>
